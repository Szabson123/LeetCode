def test(s, pattern):

    counter = {}
    list_elements = s.split()
    
    for element in list_elements:
        if element in counter:
            
    
    return list_elements

print(test('dog cat cat dog', 'abba'))